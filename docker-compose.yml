services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: nika-backend
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/app/data
      - ./frontend/public/products:/frontend/public/products
      - ./данные nika:/app/данные nika:ro
      - ./новые_фото:/app/новые_фото:ro
    environment:
      - DATABASE_URL=sqlite+aiosqlite:////app/data/nika.db
    env_file:
      - .env
    networks:
      - nika-network

  nginx:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: nika-nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      # Маппим основной конфиг
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./frontend/public/products:/usr/share/nginx/html/products
      # Прокидываем сертификаты внутрь контейнера
      - /etc/letsencrypt:/etc/letsencrypt:ro 
    depends_on:
      - backend
    networks:
      - nika-network

networks:
  nika-network:
    driver: bridge